{% extends 'base.html.twig' %}

{% block title %}Hello WishController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>List page</h1>

        <ul>
            {% for wish in wishList %}
                <li>{{ wish.title }} | <a href="{{ path('app_wish_detail', {'id': wish.id}) }}">Détail</a></li>
            {%  endfor %}
        </ul>

    </div>
{% endblock %}
